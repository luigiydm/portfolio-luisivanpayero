---
const { class: className = "", id } = Astro.props
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-24 w-full mx-auto container max-w-6xl`}
>
  <slot />
</section>
